package leetcode

import java.util.stream.Stream
import kotlin.system.measureTimeMillis

fun main() {
    val list = mutableListOf("")
    list.removeLast()
    list.removeFirst()
    synchronized(list) {

    }
    measureTimeMillis { }
     //val stream: Stream
}