package leetcode

fun subsets(nums: IntArray): List<List<Int>> {
    val subsets = mutableListOf<List<Int>>(emptyList())
    if (nums.isEmpty()) return subsets

    fun backtracking(index: Int, subset: MutableList<Int>) {
        
    }

    return subsets
}